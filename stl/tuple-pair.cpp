#include <iostream>
#include <tuple>
#include <vector>
#include <utility>
using namespace std;

int main() {
    //tuple<int, string, bool, double> t(2, "C++", true, 3.14);
    //auto t = make_tuple(3, "C++", true);
    //cout << get<2>(t) << endl;

    //pair<int, string> p(3, "c++");
    //auto p = make_pair(3, "c++");
    //cout << p.first << " " << p.second << endl;
    return 0;
}